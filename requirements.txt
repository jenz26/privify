# Production runtime installed by Streamlit Community Cloud, which reads this
# single file at the repo root. Core ML + webapp only; dev/CI tooling lives in
# requirements-dev.txt. streamlit and imageio-ffmpeg are intentionally left
# unpinned until the deploy pin-freeze step.
ultralytics~=8.3.0
opencv-python-headless~=4.10.0
numpy~=1.26.0
scikit-learn~=1.5.0
pyyaml~=6.0.0
streamlit
imageio-ffmpeg
