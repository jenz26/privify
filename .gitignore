# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
dist/
build/
*.egg-info/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Project artifacts
data/
# WIDER FACE v0.2 subset: heavy images/labels are regenerable from
# tools/prepare_wider_face.py and distributed via a GitHub Release. Only the
# data.yaml config and the small _preview/ PNGs are versioned.
datasets/wider_face_subset_v0.2/images/
datasets/wider_face_subset_v0.2/labels/
models/*.pt
models/*.pth
# Stray model weights (should be downloaded into models/ instead)
/*.pt
samples/output_*
*.log

# Test videos (downloaded at runtime or added locally for testing)
samples/*.mp4
samples/*.avi
samples/*.mov
!samples/.gitkeep
!samples/input.mp4

# Jupyter
.ipynb_checkpoints/

# Environment
.env
.env.local
.env.*.local

# Claude Code / AI tooling
.claude/
.claude-cache/
claude.local.md
.anthropic/
*.claude.md
CLAUDE.md
.cursor/
.aider*

# Test / coverage
.pytest_cache/
.coverage
htmlcov/
.ruff_cache/

# Personal study notes (not part of the public deliverable)
docs/exam_notes.md

# Local dataset archives (downloaded for upload to Releases)
*.zip

# Quarto build artifacts (kept gitignored; only final PDF is committed).
# Project root is the repo root, so these directories are created there;
# unanchored patterns also cover any docs/ subproject leftovers.
.quarto/
_freeze/

# Out-of-Distribution evaluation: extracted frames are large and fully
# regenerable from the video; ground_truth.json and figures/ are versioned
# deliverables used in the Technical Analysis Document.
evaluation/frames/
/.quarto/
**/*.quarto_ipynb
